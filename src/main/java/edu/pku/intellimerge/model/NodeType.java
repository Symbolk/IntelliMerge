package edu.pku.intellimerge.model;

public enum NodeType {
    PROJECT,
    PACKAGE,
    FILE,
    CLASS,
    INTERFACE,
    ENUM,
    INNER_CLASS,
    LOCAL_CLASS,
    FIELD,
    METHOD,
    CONSTRUCTOR
}
